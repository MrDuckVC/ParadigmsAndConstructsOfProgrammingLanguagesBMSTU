<Window
    x:Class="BackupSync.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="BackupSync">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <NavigationView x:Name="AppNavigationView"
                    PaneDisplayMode="LeftCompact"
                    IsSettingsVisible="True"
                    IsBackButtonVisible="Collapsed"
                    Loaded="AppNavigationView_Loaded"
                    SelectionChanged="AppNavigationView_SelectionChanged">

        <NavigationView.MenuItems>
            <NavigationViewItem Content="Устройства" Tag="devices">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE88E;"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <NavigationViewItem Content="Задания" Tag="jobs">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE8F2;"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <Frame x:Name="ContentFrame" Padding="24">
            <Frame.ContentTransitions>
                <TransitionCollection>
                    <NavigationThemeTransition/>
                </TransitionCollection>
            </Frame.ContentTransitions>
        </Frame>

    </NavigationView>
</Window>
